# Experience Skill group intervention

**Intervention:** apply the `frontend-productization` Skill with frozen routing.

1. Ensure the workspace is this repository (paths resolve to `frontend-productization/` and `evidence/`).
2. In Cursor, use the project skill **frontend-productization** (see `.cursor/skills/frontend-productization/SKILL.md` entrypoint) or manually follow `frontend-productization/SKILL.md`.
3. Read `frontend-productization/experience-index.json`, select **3 to 5** units per protocol §10, and inject only the chosen units’ stage text (Plan / Coding / Review / Test as applicable).
4. Record `injected_experience_count` and approximate `context_length_estimate` in the run log.

Do not paste `experiments/full-prompt-checklist.md` for this group.
