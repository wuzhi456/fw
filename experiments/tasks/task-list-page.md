# Task: Data list with search and filter (frozen text)

Build a web page that shows a searchable, filterable data table backed by a remote API. Users can type search text, choose filter options, and see matching rows update.

Do not assume any extra requirements beyond this description.
