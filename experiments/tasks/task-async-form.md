# Task: Async validated form (frozen text)

Build a form that submits data to a remote API. Some fields must be validated asynchronously against the server before submit is allowed. The user can correct errors and submit successfully.

Do not assume any extra requirements beyond this description.
