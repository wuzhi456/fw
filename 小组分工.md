# 三人分工与三周执行计划

项目最终定位：

> 一个证明 AI coding agent 可以复用成熟工程经验、并因此生成更鲁棒代码的研究原型。

最终交付不是 `FunctionWeaver` 可交互系统，而是：

1. `extract-experience` 研究型 skill 资产包。
2. `frontend-productization` / injection-router 研究型 skill 资产包，尽量带轻量可调用 router。
3. routing 有效性实验，类似 RAG hit-rate / relevance evaluation。
4. 三组对照生成实验：baseline、full-prompt、experience-skill。
5. 最终报告：问题、方法、实验结果、机制解释、威胁有效性。

---

## 1. 总体分工

| 人 | 角色 | 核心问题 | 主要产物 |
| --- | --- | --- | --- |
| A | Experience Extraction Skill / Pipeline Owner | 成熟工程经验如何被抽取、证据化、结构化？ | `extract-experience` skill、evidence audit、case study |
| B | Experience Injection / Router Owner | 经验如何被路由、少量注入，并证明 router 选得对？ | routing packets、router decision log、routing metrics、evidence-of-use |
| C | Evaluation / Report Owner | 生成质量是否变化？实验结果和结论边界是什么？ | 生成实验运行、匿名化评分、analysis/report |

一句话边界：

- A 负责“经验从哪里来，是否可信”。
- B 负责“经验如何被选中和注入，以及 routing 是否有效”。
- C 负责“生成实验如何运行、评分和报告如何可信”。

---

## 2. A：Experience Extraction Owner

### 2.1 目标

把 experience extraction 从“我们手工做过”提升为“别人可以复现、审计、扩展”的方法。

A 不需要做自动仓库挖掘系统，也不需要自动抽取全部经验。重点是研究型 skill 资产包：可复现、可审计、可人工执行。

### 2.2 必做任务

1. 创建 `extract-experience` skill 资产包。
   - 建议路径：`extract-experience/`
   - 必须包含：`SKILL.md`
   - 可选包含：`templates/`、`case-studies/`

2. 写清楚抽取输入规范。
   - 目标项目：如 react-admin、refine、Kibana。
   - 风险类：async、form、list、state、UX、responsive。
   - source type：code、docs、issue、PR、commit、test。
   - 检索范围：关键词、模块、issue/PR 标签、文件路径。

3. 写清楚抽取输出规范。
   - candidate experience
   - evidence record
   - Experience Unit draft
   - audit decision

4. 固化抽取流程。

```text
source scan
-> candidate claim
-> evidence record
-> Experience Unit draft
-> validation
-> merge / add / park / conflict
```

5. 定义 evidence 强度。
   - `strong`：evidence 直接支持经验主张。
   - `partial`：evidence 支持方向，但需要迁移解释。
   - `weak`：evidence 只能作为背景，不足以支撑主张。
   - `needs-replacement`：应更换 evidence 或降低 confidence。

6. 复核当前 18 条 Experience Units。
   - 输出 evidence audit table。
   - 重点处理低置信度项：
     - `ux-error-boundary-granularity`
     - `ux-fallback-recoverable-errors`

7. 定义新增 experience 的裁决规则。
   - 同风险类 + 同 trigger + 同 intent：`merge`
   - 同风险类 + 新 failure mode：`add`
   - evidence 弱或超出 MVP 范围：`park`
   - 与已有 EU 冲突：按 evidence strength、risk severity、specificity、stage fit 裁决

8. 负责新增 experience 的维护机制。
   - 新 experience 进入候选池前，必须检查是否与已有 EU 重复。
   - 检查维度：
     - risk class 是否相同
     - trigger 是否重叠
     - failure mode 是否相同
     - mature practice 是否只是改写
     - injection text 是否会给 agent 相同指令
   - 输出 `merge / add / park / conflict` 决策。
   - 对 `conflict` 必须写明：
     - 冲突的 EU id
     - 冲突点
     - 哪个 evidence 更强
     - 最终保留、合并或降级的理由
   - 维护一个新增经验登记表，记录每条候选经验的处理状态。

9. 做 1 个完整 extraction case study。
   - 推荐从下面选一个：
     - `form-duplicate-submit-guard`
     - `async-stale-cancel`
     - `state-stale-response-guard`
   - 必须展示：evidence -> claim -> Experience Unit -> audit decision。

### 2.3 A 的交付物

| 交付物 | 建议路径 | 验收标准 |
| --- | --- | --- |
| `extract-experience` skill | `extract-experience/SKILL.md` | 别人读完能按流程人工抽取一条新经验 |
| Evidence audit table | `extract-experience/evidence-audit-table.md` 或 `docs/` | 18 条 EU 都有 evidence strength 和 action |
| Extraction case study | `extract-experience/case-studies/*.md` | 清楚展示 evidence 到 EU 的抽象过程 |
| Merge/add/park/conflict 规则 | skill 或单独文档 | 能判断新经验应合并、加入、暂存还是冲突裁决 |
| 新增经验登记表 | `extract-experience/candidate-registry.md` 或 CSV | 每条候选经验都有 merge/add/park/conflict 决策 |

### 2.4 A 的完成标准

- `extract-experience` 达到研究型 skill 资产包标准。
- 至少一个 case study 可复现。
- 18 条 EU 的 evidence 风险被标注。
- 低置信度 EU 有处理意见：替换、降级、保留但 caveat。
- 新加入 experience 有明确的重复检测、merge 和 conflict 处理流程。

---

## 3. B：Experience Injection / Router Owner

### 3.1 目标

把 `frontend-productization` 从“一个经验 checklist/skill”升级为“可解释、可评估、尽量可调用的 routing 机制”。

B 的重点不是让 skill 更长，而是证明：

- 为什么选这些 Experience Units？
- 为什么没选另外一些？
- 选中的经验在哪个阶段影响 agent？
- skill 组代码里是否真的体现了这些经验？

### 3.2 必做任务

1. 维护现有 `frontend-productization` skill。
   - `frontend-productization/SKILL.md`
   - `frontend-productization/experience-index.json`
   - `frontend-productization/experiences/*.md`
   - `frontend-productization/templates/*.md`

2. 保持资产校验通过。

```powershell
cd FunctionWeaver
python scripts\validate_frontend_productization_skill.py
python -m json.tool frontend-productization\experience-index.json
```

3. 为三个任务生成 frozen routing packets。
   - `task-async-form`
   - `task-list-page`
   - `task-responsive-dashboard`

每份 routing packet 必须包含：

- task cues
- stage：至少 Plan / Coding
- mandatory units
- contextual top-k units
- 每条 EU 的 routing score
- 最终注入的 3-5 条 EU
- 被排除但接近 cutoff 的 near misses
- 注入 token 估计
- 每条 EU 防止的 failure mode

4. 补 router decision log。
   - 哪个 task cue 触发了该 EU？
   - 该 EU 防止什么隐性鲁棒性问题？
   - 它应在哪个阶段影响 agent：Plan / Coding / Review / Test？
   - 相似 EU 为什么被排除？

5. 尽量实现轻量 router。
   - 输入：task text + stage
   - 输出：ranked EUs + selected EUs + routing packet
   - 建议路径：`scripts/route_experience_units.py`
   - 如果 Week 1 结束仍不稳定，冻结手工 routing packets，脚本作为 future work。

6. 做 Skill vs Full Prompt intervention 一致性检查。
   - full-prompt 是否包含 skill 可能路由到的全部经验？
   - full-prompt 是否额外加入了不公平提示？
   - skill 注入文本和 full-prompt checklist 是否语义一致？

7. 做 evidence-of-use analysis。
   - 先分析已有 `task-async-form` 9 个输出。
   - 后续 C 跑完另外两个任务后补全。
   - 对每个 skill run 检查 injected EU 是否在代码中体现。

标注体现形式：

- `code-structure`
- `state-modeling`
- `ui-recovery`
- `test`
- `not-observed`

8. 定义 router failure taxonomy。
   - `missed-risk`：任务需要某经验但没选中。
   - `over-selection`：选中相关性弱的经验。
   - `under-specified-injection`：经验被选中但 stage text 太抽象。
   - `agent-noncompliance`：经验明确注入但代码没体现。
   - `full-prompt-dilution`：full-prompt 包含经验但注意力被稀释。
   - `task-irrelevance`：经验被正确排除，因为任务不相关。

9. 负责 Routing 有效性实验。
   - 这是 B 的核心评估任务，不交给 C。
   - 目标是证明 routing algorithm 本身是否有效，类似 RAG hit-rate / relevance evaluation。

10. 建立 relevance label 表。
   - 对三个任务、至少 Plan / Coding 两个 stage 标注。
   - 标注对象是 18 条 Experience Units。

标签定义：

| label | 含义 |
| --- | --- |
| 3 | 直接相关，应该注入 |
| 2 | 相关，可以作为 contextual injection |
| 1 | 弱相关，3-5 条预算下通常不应注入 |
| 0 | 不相关 |

11. 尽量做双人标注。
   - 两个 annotator 独立标注。
   - 差异 >= 2 分进入 adjudication。
   - 如果无法双人标注，标为 `silver-single-annotator` 或 `silver-spotchecked`，报告中降级说明。

12. 计算 routing metrics。
   - primary endpoint：`precision@5`
   - secondary endpoints：
     - `hit@5`
     - `recall@5`
     - `nDCG@5`
     - `mandatory_hit_rate`
     - `near_miss_analysis`
   - exploratory：`@3` 版本和 Review/Test stage。

13. 输出 routing evaluation summary。
   - 哪些任务选得准？
   - 哪些 EU 是 near miss？
   - router 失败属于哪类 taxonomy？

### 3.3 B 的交付物

| 交付物 | 建议路径 | 验收标准 |
| --- | --- | --- |
| Routing packets | `experiments/routing/routing-packets/*.md` | 三个任务都有 packet，至少覆盖 Plan/Coding |
| Router decision log | `experiments/routing/router-decision-log.md` | 每个 selected EU 都有选择理由和 failure mode |
| Routing results CSV | `experiments/routing/routing-results.csv` | 可用于 C 计算 hit@k / precision@k |
| Routing labels | `experiments/routing/relevance-labels.csv` | 三任务 * 至少 Plan/Coding * 18 EU 有标签 |
| Routing metrics | `experiments/routing/routing-metrics.csv` | 至少有 precision@5、hit@5、recall@5、nDCG@5 |
| 轻量 router | `scripts/route_experience_units.py` | 可选；若不稳定则不阻塞 |
| Full Prompt 一致性检查 | `experiments/routing/full-prompt-consistency.md` | 说明对照组是否公平 |
| Evidence-of-use table | `experiments/evidence-of-use.md` 或 CSV | 说明 injected EU 是否体现在代码里 |

### 3.4 B 的完成标准

- `frontend-productization` 校验通过。
- 三个任务都有 frozen routing packet。
- routing packet 可以解释“为什么选”和“为什么不选”。
- 至少已有 `task-async-form` 的 evidence-of-use 分析。
- 如果 router 脚本未完成，必须有手工 packet 和 README，不能影响实验。
- routing 有效性实验由 B 完成，并能独立说明 router 是否选得对。

---

## 4. C：Evaluation / Report Owner

### 4.1 目标

把项目从“看起来有用”提升为“有生成实验、有评分、有机制解释、有边界”的研究结果。

C 不负责改经验内容，也不负责 routing eval 的具体执行，避免评价污染。C 负责生成实验、验证、评分、分析和报告整合。C 在最终报告中引用 B 的 routing metrics。

### 4.2 必做任务一：生成实验执行

实验组：

- Baseline
- Full Prompt
- Experience Skill

任务：

- `task-async-form`
- `task-list-page`
- `task-responsive-dashboard`

目标矩阵：

```text
3 tasks * 3 groups * 3 reps = 27 accounted runs
```

当前状态：

- `task-async-form` 已有 9 次 exploratory runs。
- 但已有 run-log 存在缺字段：
  - `context_length_estimate`
  - `start_time`
  - `end_time`
  - `duration_minutes`
- 且 async 批次执行顺序与当前 `randomization-table-27.csv` 的 async slots 不一致。

处理规则：

1. 优先从 `metadata.json`、日志、文件时间、batch notes 回填。
2. 如果无法可靠回填，标记 `VALID_WITH_LIMITATIONS`。
3. 对 async 批次记录 protocol deviation。
4. 不基于这些行做 timing/context-length 强结论。

剩余运行：

- 跑满 `task-list-page` 的 9 个 runs。
- 跑满 `task-responsive-dashboard` 的 9 个 runs。
- 按随机化表执行；如果偏离，先记录 deviation。

### 4.3 必做任务二：Build / Smoke / Stress 验证

每个生成输出尽量执行：

```powershell
npm install
npm run build
```

如项目提供脚本，再执行：

```powershell
npm run lint
npm run typecheck
npm test
```

Smoke checks：

- 页面能启动或静态 build 可 serve。
- 主页面能渲染。
- 主要交互不是纯静态占位。
- 没有阻塞主流程的 runtime error。

Stress checks：

| 任务 | Stress 场景 |
| --- | --- |
| async form | 慢验证、失败验证、失败提交、重复提交、纠错后提交、stale validation response |
| list page | 空数据、大数据、快速搜索/筛选切换、失败请求/重试、分页或 bounded rendering、长文本 |
| responsive dashboard | desktop、小屏 laptop、窄屏、长标签、密集操作、局部错误状态 |

输出表：

```csv
task_id,group,rep,check_id,status,evidence,notes
```

status 可用：

- `pass`
- `fail`
- `not_applicable`
- `not_testable`

### 4.4 必做任务三：匿名化与评分

匿名化规则：

1. 将每个 scorable output 复制到 `experiments/anonymous-submissions/<anonymous_id>/`。
2. 去掉路径、目录名、metadata 中的组别信息。
3. reviewer 只能看到：
   - frozen task text
   - generated output
   - build/run instructions
   - rubric/scoring sheet
4. reviewer 不能看到：
   - group label
   - intervention file
   - routing packet
   - run-log row
   - `baseline` / `full-prompt` / `experience-skill` 路径名
   - execution order

评分：

- 至少两个 reviewer 独立评分。
- 单项差异 >= 2 分进入 adjudication。
- 如果可行，计算 Cohen's kappa。
- 如果双 reviewer 没完成，报告中明确标注为 single-review 或 LLM-assisted supplementary，不能声称 inter-rater reliability。

### 4.5 必做任务四：最终报告

报告必须包含：

1. 问题：成熟工程经验能否提升 AI coding agent 生成质量？
2. 方法：Experience-Augmented Coding。
3. 抽取机制：evidence-backed experience -> Experience Unit。
4. 注入机制：routing algorithm -> 3-5 条经验注入。
5. Routing 有效性实验：precision@5 等指标。
6. 生成实验：三任务、三组、重复运行、build/smoke/stress、rubric。
7. 机制解释：
   - evidence 支撑了什么经验？
   - router 为什么选中？
   - skill 组代码哪里体现？
   - full-prompt 为什么可能不稳定？
   - 失败案例说明什么边界？
8. 威胁有效性：
   - 样本量小
   - 平台参数不可完全锁定
   - reviewer 主观性
   - routing label 主观性
   - async 早期批次 ledger 不完整

### 4.7 C 的交付物

| 交付物 | 建议路径 | 验收标准 |
| --- | --- | --- |
| Run ledger | `experiments/run-log.csv` | 所有 accounted runs 有状态和缺失说明 |
| Validation table | `experiments/validation-results.csv` | build/smoke/stress 有记录 |
| Anonymous packets | `experiments/anonymous-submissions/` | 无明显组别泄漏 |
| Scores | `experiments/scores.csv` 或同等文件 | 可区分 formal / supplementary |
| Analysis | `experiments/analysis.md` | 统计和机制解释清楚 |
| Final report | `docs/experience-augmented-vibe-coding-report.md` | 如实汇报结果和边界 |

---

## 5. 三周时间表

### Week 1：方法与 Router 打底

| 人 | 本周任务 | 周末验收 |
| --- | --- | --- |
| A | 完成 `extract-experience` skill、evidence audit、1 个 case study | 别人能复现一条经验抽取 |
| B | 完成三任务 routing packets、router decision log、full-prompt 一致性检查、routing labels 和第一版 routing metrics | 每个任务能解释选中 3-5 条 EU 的原因，并有 routing hit-rate / relevance evaluation 表 |
| C | 冻结生成实验 primary/secondary endpoints，准备 run-log / validation / anonymous packet 流程 | 生成实验执行路径清楚，可直接跑 Week 2 |

Week 1 共同 checkpoint：

- `frontend-productization` 校验通过。
- routing packet、label、metric 三者能互相对上，且由 B 负责维护。
- 如果 router 脚本不稳定，立即降级为手工 packet + CSV 计算，不拖延实验。

### Week 2：实验执行与验证

| 人 | 本周任务 | 周末验收 |
| --- | --- | --- |
| A | 支持解释低置信 EU，补 final report 的 extraction/method 部分 | 方法部分有证据链说明 |
| B | 对新跑出的 skill 组输出做 evidence-of-use 分析，并把 routing metrics 更新到完整任务集 | skill 注入是否体现在代码里有记录，routing eval 完整 |
| C | 跑剩余两个任务的 18 次 runs，做 build/smoke/stress，准备匿名包 | 27 accounted runs 或清楚的 deviation/failure log |

Week 2 共同 checkpoint：

- run-log 更新。
- validation-results 更新。
- anonymous-submissions 准备好。
- async 旧批次的 metadata 缺口已 backfill 或标记 `VALID_WITH_LIMITATIONS`。

### Week 3：评分、分析与报告

| 人 | 本周任务 | 周末验收 |
| --- | --- | --- |
| A | 完成 extraction case study 和 evidence 可信度解释 | 报告能回答“经验从哪里来，为什么可信” |
| B | 完成 mechanism case study：router -> code evidence | 报告能回答“为什么选中、代码哪里体现” |
| C | 完成评分汇总、统计、威胁有效性和最终报告整合 | 报告可提交，可答辩 |

Week 3 共同 checkpoint：

- routing metrics 已进入报告。
- generation results 已进入报告。
- formal / supplementary scoring 被清楚区分。
- 所有强结论都有证据支撑。
- mixed / negative / limitation 结果如实保留。

---

## 6. 相互依赖关系

```text
A: evidence audit / extraction method / new-experience merge-conflict maintenance
        ↓
B: Experience Units / routing packets / router decision log / routing relevance labels + metrics
        ↓
C: generation experiment + validation + scoring
        ↓
A+B+C: mechanism explanation + final report
```

关键依赖：

- B 的 routing packets 依赖 A 对 EU 和 evidence 可信度的复核。
- B 的 routing hit-rate 实验依赖自己输出稳定的 routing-results，并可邀请 A/C 做标注或 spot-check。
- C 的生成实验不依赖 router 脚本是否完成，只依赖 frozen routing packets。
- 最终报告的机制解释必须由 A、B、C 三方共同提供证据。

---

## 7. 降级规则

为了保证三周内能交付，以下情况不阻塞项目，但必须如实标注：

| 触发条件 | 降级处理 | 报告边界 |
| --- | --- | --- |
| router 脚本 Week 1 不稳定 | 使用手工 routing packets + CSV metrics | 自动化作为 future work |
| routing 双人标注未完成 | 使用 silver labels 或 spot-check labels | 不声称强 human-consensus routing validity |
| 剩余 runs Week 2 未全部完成 | 报告 accounted runs 和 missing cells | 不声称完整 27-run matrix |
| 双 reviewer Week 3 未完成 | 使用 single-review 或 LLM-assisted supplementary | 不声称 inter-rater reliability |
| stress 自动化成本太高 | 使用手工 checklist 和 evidence notes | stress evidence 标为 qualitative/spot-check |
| async 旧批次 metadata 无法回填 | 标记 `VALID_WITH_LIMITATIONS` | 不做 timing/context-length 强结论 |
| async 旧批次随机化不匹配未重跑 | 记录 protocol deviation | 不声称完整矩阵严格随机顺序 |

---

## 8. 最终提交检查清单

### A 检查

- [ ] `extract-experience/SKILL.md` 存在。
- [ ] evidence audit table 完成。
- [ ] 至少 1 个 extraction case study 完成。
- [ ] 低置信 EU 有处理意见。
- [ ] 新增 experience 的 merge/add/park/conflict 规则和登记表完成。

### B 检查

- [ ] `frontend-productization` 校验通过。
- [ ] 三个任务 routing packets 完成。
- [ ] routing-results.csv 完成。
- [ ] relevance-labels.csv 完成。
- [ ] routing-metrics.csv 完成。
- [ ] router decision log 完成。
- [ ] full-prompt 一致性检查完成。
- [ ] evidence-of-use 分析至少覆盖 skill runs。

### C 检查

- [ ] run-log.csv 更新并记录 deviation。
- [ ] validation-results.csv 完成。
- [ ] anonymous-submissions 准备好。
- [ ] scoring / analysis 文件完成。
- [ ] final report 完成。

### 全组检查

- [ ] 没有做 FunctionWeaver UI。
- [ ] 结果如实汇报，没有硬性成功门槛。
- [ ] routing 有效性和 generation 质量分开评估。
- [ ] final report 能解释 evidence -> EU -> router -> generated code。
