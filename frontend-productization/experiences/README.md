# Experience Units corpus

**Status:** empty (cleared 2026-05-24)

Manual EU files were removed. Regenerate with:

1. Read `extract-experience/prompts/regenerate-eu-corpus.md`
2. Invoke `/extract-experience` per batch
3. Run `python scripts/validate_frontend_productization_skill.py` after each promotion

Target: **18 EUs** (6 risk classes × 3 each) unless scope is intentionally reduced.
